services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: edoc
      POSTGRES_PASSWORD: edoc
      POSTGRES_DB: edoc
    ports:
      - "5432:5432"
    volumes:
      - edoc_pg_data:/var/lib/postgresql/data

volumes:
  edoc_pg_data:
